import React from "react";
import Carrito from "../assets/carrito.png";

const CartWidget = () => {
  return <img src={Carrito} alt="Carrito" width={20} height={20} />;
};

export default CartWidget;
